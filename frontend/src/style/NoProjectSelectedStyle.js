import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    body:{
        position: "absolute",
        background:"black",        
        top:"0",
        left:"0",
        width:"100%",
        height:"100%"
    }
  }));

  export default useStyles;
  