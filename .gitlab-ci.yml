stages:
  - build
  - docker-build
  - unit-test
  - deploy

before_script:
  - export GRADLE_USER_HOME=`pwd`/.gradle # from https://stackoverflow.com/a/36050711

cache: &global_cache
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - backend/build
    - .gradle
    - .npm
    - frontend/build
    - frontend/node_modules
  policy: pull-push

build:backend:
  stage: build
  script:
    - ./gradlew assemble
  image: gradle:jre15

build:frontend:
  stage: build
  script:
    - cd frontend
    - npm ci --cache .npm --prefer-offline
    - npm run build
  image: node:lts-alpine

docker-build:
  stage: docker-build
  needs:
    - build:backend
    - build:frontend
  script:
    - docker build -t gitlabanalyzer:$CI_COMMIT_REF_SLUG .
  image: docker
  cache:
    <<: *global_cache
    policy: pull

unit-test:backend:
  stage: unit-test
  needs:
    - build:backend
  script:
    - ./gradlew test
  image: gradle:jre15
  cache:
    <<: *global_cache
    policy: pull
  artifacts:
    reports:
      junit: backend/build/test-results/test/**/TEST-*.xml

deploy:dev:
  stage: deploy
  needs:
    - docker-build
  script:
    - docker-compose -f docker-compose.yml -f docker-compose.dev.ym -p gitlabanalyzer_dev stop
    - docker-compose -f docker-compose.yml -f docker-compose.dev.ym -p gitlabanalyzer_dev up -d
  cache:
    <<: *global_cache
    policy: pull
  image: docker/compose
  when: manual